# Conway’s Game of Life (Simple C Implementation)

A simple implementation of Conway’s Game of Life in C.
It follows the default B3/S23 rule, uses an array instead of a matrix and simply prints the cells directly with printf().

### Compiling
```bash
gcc simple-conway.c -o simple-conway
```
### Running
```bash
./simple-conway
```
